{
  "env": {
    "TEMP": "/tmp",
    "NEXT_TELEMETRY_DISABLED": "1", 
    "NODE_OPTIONS": "--max_old_space_size=4096",
    "SKIP_DEPLOY_CHECK": "1"
  },
  "buildCommand": "mkdir -p temp && SKIP_DEPLOY_CHECK=1 TEMP=/tmp next build && mkdir -p .next/server/app/\\(home\\) && touch .next/server/app/\\(home\\)/page_client-reference-manifest.js",
  "outputDirectory": ".next"
} 