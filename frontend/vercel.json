{
  "env": {
    "TEMP": "/tmp",
    "NEXT_TELEMETRY_DISABLED": "1", 
    "NODE_OPTIONS": "--max_old_space_size=4096"
  },
  "buildCommand": "mkdir -p temp && TEMP=/tmp next build",
  "outputDirectory": ".next"
} 