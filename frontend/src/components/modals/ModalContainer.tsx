'use client';

// This component is no longer needed as the ModalProvider already handles rendering modals
// We're keeping it as a placeholder to avoid breaking imports
export default function ModalContainer() {
    // The actual modal rendering is now handled directly in the ModalProvider
    return null;
} 